import json

def count_data(data, key):
    return len(data[key])
    # how many items
    # FINISH THIS TO RETURN THE NUMBER OF VALUES.



def get_data_value(data,key):
    # retrieve the value of "totalResults()" in walmart...json (25000 or so)
    return data[key]
    # look for the key work and return the value
